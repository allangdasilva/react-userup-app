import UserCard from "./ui/UserCard";

const Users = () => {
  return (
    <section className="flex justify-center py-8 rounded-xl bg-indigo-200">
      <UserCard />
    </section>
  );
};

export default Users;
